# LINE OA Integration Guide (CraftFlow)

## 📌 Overview

ระบบนี้ช่วยให้สามารถเชื่อมต่อกับ **LINE Official Account (OA)** เพื่อดึงข้อมูลลูกค้าอัตโนมัติเมื่อลูกค้าทักแชทเข้ามา หรือกดเพิ่มเพื่อน และนำข้อมูลเหล่านั้นมาใช้ในการสร้าง **ใบเสนอราคา** (Quotation) หรือ **เพิ่มลูกค้า** (Customer) ได้ทันที โดยไม่ต้องพิมพ์เอง

---

## 🛠️ Setup Instructions (การตั้งค่า)

### 1. LINE Developers Console

1. เข้าไปที่ [LINE Developers Console](https://developers.line.biz/console/).
2. สร้าง **Provider** และ **Messaging API Channel**.
3. ไปที่ Tab **Messaging API**:
    * **Channel Access Token**: กด Issue เพื่อรับ Token (เก็บไว้ใช้ใน env).
    * **Webhook URL**: ใส่ URL ของระบบเรา เช่น `https://your-domain.com/api/webhooks/line` (ต้องใช้ domain จริง หรือ ngrok สำหรับ test).
    * **Webhook**: เปลี่ยนเป็น `Enabled`.
4. ไปที่ Tab **Basic Settings**:
    * **Channel Secret**: กดรับค่า (เก็บไว้ใช้ใน env).

### 2. Environment Variables (.env.local)

เพิ่มค่าเหล่านี้ในไฟล์ `.env` ที่ root ของโปรเจกต์:

```env
LINE_CHANNEL_ACCESS_TOKEN="YOUR_LONG_ACCESS_TOKEN_HERE"
LINE_CHANNEL_SECRET="YOUR_CHANNEL_SECRET_HERE"
```

---

## 🗄️ Database Schema (Supabase)

### Table: `line_users`

ตารางนี้จะถูก Sync อัตโนมัติเมื่อมี Webhook event เข้ามา

| Column | Type | Description |
| :--- | :--- | :--- |
| `line_user_id` | text (PK) | User ID จาก LINE (Uxxxxxxxx...) |
| `display_name` | text | ชื่อ LINE ที่แสดงในแอป |
| `picture_url` | text | ลิงก์รูปโปรไฟล์ |
| `status_message` | text | ข้อความสถานะ (Bio) |
| `is_friend` | boolean | สถานะความเป็นเพื่อน (True = Follow, False = Unfollow) |
| `last_interaction` | timestamp | เวลาที่คุยล่าสุด |
| `created_at` | timestamp | - |
| `updated_at` | timestamp | - |

---

## 📡 Webhook Handling (`/api/webhooks/line`)

ระบบรองรับ Event จาก LINE ดังนี้:

1. **Message Event**: เมื่อลูกค้าพิมพ์ข้อความมา -> ระบบจะบันทึก/อัปเดตข้อมูลลูกค้าใน `line_users` ทันที.
2. **Follow Event**: เมื่อลูกค้ากดเพิ่มเพื่อน -> บันทึกข้อมูลและตั้ง `is_friend = true`.
3. **Unfollow Event**: เมื่อลูกค้าบล็อก -> ตั้ง `is_friend = false`.

**File Location**: `app/api/webhooks/line/route.ts`

---

## 🧩 Frontend Components

### 1. `LineUserSearch`

คอมโพเนนต์สำหรับค้นหาและเลือกผู้ใช้ LINE ใช้ในฟอร์มต่างๆ

**Features:**

* **Recent Users**: แสดงรายการคนที่เพิ่งทักมาล่าสุด 10 อันดับแรกทันทีที่เปิด.
* **Search**: ค้นหาจากชื่อ (Display Name) ได้.
* **Selection**: เมื่อเลือก จะคืนค่า object `LineUser` กลับไปให้ parent component ใช้งานต่อ.

**Usage Example:**

```tsx
import { LineUserSearch } from '@/components/customers/LineUserSearch'

// ... inside component
<LineUserSearch 
    onSelect={(user) => {
        console.log("Selected LINE User:", user)
        // user.display_name, user.picture_url, etc.
    }} 
/>
```

### 2. Integration Points

* **หน้าสร้างใบเสนอราคา (`/billing/new`)**: มีปุ่มค้นหา LINE เพื่อ Import ลูกค้ามาลงฟอร์มได้เลย.
* **หน้าจัดการลูกค้า (`/customers`)**: ใน Popup เพิ่มลูกค้า มีช่องค้นหา LINE เพื่อผูกข้อมูล.

---

## 🔄 Workflow การใช้งานจริง

1. ลูกค้าทัก LINE OA ของร้านมา ("สวัสดีครับ/ค่ะ").
2. Webhook ทำงาน -> บันทึกข้อมูลลูกค้าลง `line_users` ใน Database.
3. Admin เข้าเว็บ -> ไปที่หน้า **สร้างใบเสนอราคา**.
4. กด **ค้นหาลูกค้า** -> เลือก **Import from LINE**.
5. เลือกชื่อลูกค้าที่เพิ่งทักมา -> ข้อมูลชื่อจะถูกกรอกลงใบเสนอราคาให้อัตโนมัติ!

---

## ⚠️ Troubleshooting

* **หาลูกค้าไม่เจอ?**: ลูกค้าต้องทักแชทมาอย่างน้อย 1 ครั้งหลังจากติดตั้งระบบ หรือต้องกด Unblock/Block ใหม่เพื่อให้เกิด Event.
* **Webhook Error?**: ตรวจสอบ `LINE_CHANNEL_ACCESS_TOKEN` ว่าถูกต้องและยังไม่หมดอายุ.
* **รูปไม่ขึ้น?**: ตรวจสอบว่า `picture_url` จาก LINE ยัง valid อยู่ (บางทีมี expire).
