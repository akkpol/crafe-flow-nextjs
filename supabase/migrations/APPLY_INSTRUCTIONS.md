# T-DB-001 Migration Apply Instructions

**Generated:** 2026-03-09  
**Project:** CraftFlow ERP  
**Project ID:** pucxyzgjwkkkbgwbjxuy

---

## 📁 Migration Files Created

| File | Wave | Description |
|------|------|-------------|
| `000_master_runner.sql` | All-in-one | Complete migration (run this for quick setup) |
| `001_wave_a_missing_fields.sql` | A | Add missing columns to existing tables |
| `002_wave_b_phase1_tables.sql` | B | Create CustomerLocation & Attachment tables |
| `003_wave_c_orderstatus_phase2.sql` | C | Create OrderStatusConfig & Phase 2 tables |
| `004_wave_d_indexes.sql` | D | Performance indexes |
| `005_wave_e_rls_policies.sql` | E | Row Level Security policies |

---

## 🚀 How to Apply Migrations

### Option 1: Quick Apply (Recommended for dev)
Run the master runner file in Supabase SQL Editor:

1. Go to https://supabase.com/dashboard/project/pucxyzgjwkkkbgwbjxuy
2. Navigate to **SQL Editor** → **New Query**
3. Copy contents of `000_master_runner.sql`
4. Run the query
5. Check output for "T-DB-001: ALL MIGRATIONS COMPLETED SUCCESSFULLY"

### Option 2: Wave-by-Wave (Safer for production)

Run each file in order and verify after each wave:

```sql
-- Step 1: Run 001_wave_a_missing_fields.sql
-- Verify: Check new columns exist
SELECT column_name 
FROM information_schema.columns 
WHERE table_name = 'Customer' AND column_name = 'email';

-- Step 2: Run 002_wave_b_phase1_tables.sql
-- Verify: Check tables created
SELECT * FROM "CustomerLocation" LIMIT 1;
SELECT * FROM "Attachment" LIMIT 1;

-- Step 3: Run 003_wave_c_orderstatus_phase2.sql
-- Verify: Check OrderStatusConfig seeded
SELECT * FROM "OrderStatusConfig" WHERE isDefault = TRUE;

-- Step 4: Run 004_wave_d_indexes.sql
-- Verify: Check indexes
SELECT indexname FROM pg_indexes WHERE tablename = 'Customer';

-- Step 5: Run 005_wave_e_rls_policies.sql
-- Verify: Check RLS enabled
SELECT relname, relrowsecurity FROM pg_class WHERE relname = 'Customer';
```

---

## ⚠️ Pre-Flight Checklist

Before applying:
- [ ] Backup database (if production)
- [ ] Test on staging first
- [ ] Ensure no active connections during migration
- [ ] Have rollback plan ready

---

## ✅ Post-Migration Verification

After applying, run these checks:

```sql
-- 1. Verify all new tables exist
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN ('CustomerLocation', 'Attachment', 'OrderStatusConfig', 'ApprovalWorkflow', 'Notification', 'SystemSettings');

-- 2. Verify OrderStatusConfig has default rows
SELECT statusKey, labelThai, isDefault 
FROM "OrderStatusConfig" 
WHERE isDefault = TRUE 
ORDER BY sortOrder;

-- 3. Verify RLS is enabled
SELECT relname, relrowsecurity 
FROM pg_class 
WHERE relname IN ('Customer', 'Order', 'Quotation', 'Invoice', 'Material');

-- 4. Verify new columns exist
SELECT table_name, column_name 
FROM information_schema.columns 
WHERE column_name IN ('email', 'companyName', 'progressPercent', 'approvalStatus');
```

---

## 🔧 Generating TypeScript Types

After migrations are applied, generate updated types:

```bash
# Using Supabase CLI
supabase gen types typescript --project-id pucxyzgjwkkkbgwbjxuy --schema public > lib/database.types.ts

# Or via npm script (if configured)
npm run db:types
```

---

## 📊 Migration Summary

| Metric | Count |
|--------|-------|
| New tables | 6 (CustomerLocation, Attachment, OrderStatusConfig, ApprovalWorkflow, Notification, SystemSettings) |
| Modified tables | 7 (Customer, Material, DesignFile, Order, Quotation, StockTransaction, Receipt) |
| New columns | 30+ |
| New indexes | 20+ |
| RLS policies | 20+ |

---

## 🎯 Next Steps After Migration

1. **Integration Agent (T-INT-001):** Update `lib/types.ts` with new tables
2. **Backend Agent (T-BE-001):** Create `actions/settings.ts` for OrderStatusConfig CRUD
3. **Frontend Agent (T-FE-001):** Create `app/settings/statuses/` UI

---

*Generated by Database Agent | T-DB-001 Complete*
